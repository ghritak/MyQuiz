import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  heading: {
    fontSize: 20,
    fontWeight: '600',
  },
  buttonTextWhite: {
    color: 'white',
    fontWeight: '600',
  },
});
